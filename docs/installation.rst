============
Installation
============

At the command line::

    $ easy_install django-pj-portfolio

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pj-portfolio
    $ pip install django-pj-portfolio
